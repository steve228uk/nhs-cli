# Non-secret configuration only. The CLI does not load dotenv files.
# NHS_CLI_STORAGE=keyring
# Headless: explicitly select encrypted-file and have a secret manager inject
# NHS_CLI_STATE_KEY and, if needed, NHS_CLI_CREDENTIALS into the process environment.
# Never put secret values in this file or a .env file.

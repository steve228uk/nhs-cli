# Prefer macOS Keychain to an environment variable. If an agent runtime injects
# credentials, it must provide one JSON object and keep this file untracked.
NHS_PRESCRIPTIONS_CREDENTIALS='{"email":"you@example.com","password":"replace-me"}'
